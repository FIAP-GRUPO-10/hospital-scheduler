spring.application.name=agendamento
spring.main.banner-mode=off
server.port=8080

# Kafka Configuration
spring.kafka.bootstrap-servers=kafka:9092

# Producer - envia mensagens em JSON
spring.kafka.producer.key-serializer=org.apache.kafka.common.serialization.StringSerializer
spring.kafka.producer.value-serializer=org.springframework.kafka.support.serializer.JsonSerializer

# Consumer - recebe mensagens em JSON
spring.kafka.consumer.group-id=agendamento-group
spring.kafka.consumer.key-deserializer=org.apache.kafka.common.serialization.StringDeserializer
spring.kafka.consumer.value-deserializer=org.springframework.kafka.support.serializer.JsonDeserializer
spring.kafka.consumer.properties.spring.json.trusted.packages=*
spring.kafka.consumer.auto-offset-reset=earliest

# Admin - não deixar auto-criar tópicos
spring.kafka.admin.auto-create=false
spring.kafka.admin.properties.bootstrap.servers=kafka:9092

# Jackson - garantir que datas sejam serializadas como ISO-8601
spring.jackson.serialization.write-dates-as-timestamps=false
